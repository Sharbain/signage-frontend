✅ STEP 1 — Modify Devices.jsx

Open your file:

src/pages/Devices.jsx


Delete everything inside and replace with the following:

import { Monitor, Wifi, Power, Camera, MoreHorizontal, Plus } from "lucide-react";

export default function Devices() {

  // Temporary demo list (later this comes from API)
  const devices = [
    {
      id: 1,
      name: "Reception Screen",
      location: "Main Building — Lobby",
      status: "online",
      heartbeat: "2 minutes ago",
      data: "450 MB / month",
      thumbnail: "https://via.placeholder.com/300x180.png?text=Preview",
    },
    {
      id: 2,
      name: "Lab Waiting Area",
      location: "2nd Floor — Lab 5",
      status: "offline",
      heartbeat: "Last seen 3 hours ago",
      data: "0 MB (offline)",
      thumbnail: "https://via.placeholder.com/300x180.png?text=No+Signal",
    },
  ];

  return (
    <div>

      {/* Header */}
      <div className="flex justify-between items-center mb-8">
        <h1 className="text-3xl font-bold text-primary">Devices</h1>

        <button className="flex items-center gap-2 bg-primary text-white px-4 py-2 rounded-xl shadow hover:bg-primary/80 transition">
          <Plus size={18} />
          Add Device
        </button>
      </div>

      {/* Search */}
      <input
        type="text"
        placeholder="Search devices..."
        className="w-full mb-8 p-3 border border-gray-300 rounded-xl shadow-sm focus:outline-primary"
      />

      {/* Device Grid */}
      <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-6">

        {devices.map((device) => (
          <div
            key={device.id}
            className="bg-white rounded-2xl shadow p-5 flex flex-col gap-4 hover:shadow-lg transition"
          >
            {/* Thumbnail */}
            <img
              src={device.thumbnail}
              alt="device preview"
              className="rounded-xl w-full h-40 object-cover"
            />

            {/* Device Name */}
            <div className="flex justify-between items-center">
              <h2 className="text-xl font-semibold text-text">{device.name}</h2>

              {/* Online/Offline dot */}
              <span
                className={`w-3 h-3 rounded-full ${
                  device.status === "online" ? "bg-green-500" : "bg-red-500"
                }`}
              ></span>
            </div>

            {/* Location */}
            <p className="text-gray-500">{device.location}</p>

            {/* Status info */}
            <div className="flex flex-col gap-2 text-sm text-gray-700">
              <div className="flex items-center gap-2">
                <Wifi size={16} />
                <span>Last heartbeat: {device.heartbeat}</span>
              </div>

              <div className="flex items-center gap-2">
                <Monitor size={16} />
                <span>Data used: {device.data}</span>
              </div>
            </div>

            {/* Actions */}
            <div className="flex justify-between mt-4">

              <button className="flex items-center gap-2 bg-gray-100 px-3 py-2 rounded-xl hover:bg-gray-200 transition">
                <Camera size={16} />
                Screenshot
              </button>

              <button className="flex items-center gap-2 bg-red-100 text-red-600 px-3 py-2 rounded-xl hover:bg-red-200 transition">
                <Power size={16} />
                Reboot
              </button>

              <button className="p-2 rounded-xl hover:bg-gray-100">
                <MoreHorizontal size={20} />
              </button>

            </div>

          </div>
        ))}

      </div>
    </div>
  );
}